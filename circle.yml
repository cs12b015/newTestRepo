##Test commands
test:
  override:
    - npm test

## Deployment commands
deployment:
  production: # just a label; label names are completely up to you
    branch: master
    commands:
      - chmod +x deploy.sh
      - ./deploy.sh

